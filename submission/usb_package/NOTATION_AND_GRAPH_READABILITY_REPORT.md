# Notation And Graph Readability Report

## Files Checked

- `solution/solution_1.ipynb`
- `solution/README.md`
- `solution/figures/`
- `solution/tables/`
- `README.md`
- `submission/usb_package/presentation/PRESENTATION_SLIDE_TEXT_EN.md`
- `submission/usb_package/presentation/PRESENTATION_STORYBOARD_EN.md`
- `submission/usb_package/JUDGING_CRITERIA_ALIGNMENT.md`
- `submission/usb_package/source_code/README_FOR_JUDGES.md`
- `submission/usb_package/source_code/CODE_MANIFEST.md`
- `submission/usb_package/source_code/PROBLEM_REQUIREMENT_MAP.md`
- `submission/usb_package/source_code/REPRODUCIBILITY_COMMANDS.md`
- `submission/usb_package/source_code/results/ibm_qpu_validation/`

## Labels Replaced Or Clarified

| Earlier wording | Final-facing clarification |
| --- | --- |
| `mmd`, `MMD` alone | Fidelity-kernel MMD, \(D_{\mathrm{MMD}}\) |
| `wasserstein`, `WassDist`, `wass` | Wasserstein-type distance, \(W_{1-F}\) |
| `S0` | \(S_0\), initial target ensemble near \(|00\rangle\) |
| `St` or Hamiltonian output | \(S_t^{\mathrm{Ham}}\), projected ensemble after fixed-Hamiltonian time \(t\) |
| `random step` | random-unitary layer \(k\) |
| `P2 baseline`, `fixed H` | Hamiltonian projected diffusion under fixed \(H\) (Problem 2 baseline) |
| `gain` | denoising distance gain, \(\Delta D\) |
| `success` | post-selection success probability, \(p_{\mathrm{succ}}\) |
| `diversity` | diversity retention proxy, \(R_{\mathrm{div}}\) |
| `beta sweep` | measurement-basis angle \(\beta\) sweep |
| `qpu` | IBM QPU hardware-execution validation |

Remaining code-level names may still appear inside implementation scripts or
generated support summaries. They are not used as unexplained final-facing
labels in the final notebook/presentation.

## Figures Regenerated Or Added

Added:

- `solution/figures/fig_p2_fixed_h_baseline_visible.png`
- `solution/figures/fig_metric_aligned_comparison_readable.png`

Regenerated for readability:

- `solution/figures/fig2_random_unitary_haar_baseline.png`

Regeneration command:

```powershell
python scripts/create_solution_haar_baseline.py
python scripts/create_readable_problem2_figures.py
```

These figures use existing deterministic result data only. No new experimental
data was created.

## Problem 1(c) Haar Baseline Visibility

Resolved. The Haar reference mean is now a thicker black dashed line, the
one-standard-deviation band is highlighted in gold, and a lower zoom row shows
the \(k \geq 1\) plateau region where the random-unitary trajectory overlaps
the Haar reference level.

## P2 Fixed-H Baseline Visibility

Resolved. The fixed-Hamiltonian projected diffusion baseline is now visible in
two ways:

1. A standalone fixed-\(H\) panel shows \(D_{\mathrm{MMD}}(S_t^{\mathrm{Ham}},S_0)\)
   and \(W_{1-F}(S_t^{\mathrm{Ham}},S_0)\) versus Hamiltonian time \(t\).
2. The metric-aligned comparison redraws \(S_t^{\mathrm{Ham}}\) after the
   random-unitary trajectory with larger square markers, thicker lines, hatch
   styling in the bar panel, and legends placed outside the plot area.

## Notebook And Presentation Updates

- `solution/solution_1.ipynb` now includes a `Notation used in figures` table.
- Problem 1(c), Problem 2(c), Problem 2(d), Problem 3(b), Problem 3(c), and IBM
  QPU appendix captions now use the standardized notation.
- `PRESENTATION_SLIDE_TEXT_EN.md` includes the notation table and updated
  figure paths.
- `PRESENTATION_STORYBOARD_EN.md` uses the same \(S_k^{\mathrm{RU}}\),
  \(S_t^{\mathrm{Ham}}\), \(p_{\mathrm{succ}}\), and \(R_{\mathrm{div}}\) labels.

## Remaining Ambiguous Labels

No blocking ambiguous labels remain in the final notebook, README, or
presentation text. Some support artifacts generated by older scripts still
contain code-oriented column names such as `continuous_diversity_retention`;
these are retained as traceability artifacts and are interpreted by the final
notebook notation table.
